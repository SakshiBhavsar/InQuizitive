
server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sys
    username: root
    password: your_password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    org.hibernate: info
